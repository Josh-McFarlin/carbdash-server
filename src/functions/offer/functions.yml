createOffer:
  handler: src/functions/offer/offer.createOffer
  events:
    - http:
        path: offer/create
        method: post
        cors: true
        authorizer:
          name: offerAuthorizer
          identitySource: method.request.header.Authorization
findOffers:
  handler: src/functions/offer/offer.findOffers
  events:
    - http:
        path: offer/find
        method: get
        cors: true
        authorizer:
          name: offerAuthorizer
          identitySource: method.request.header.Authorization
findOfferById:
  handler: src/functions/offer/offer.findOfferById
  events:
    - http:
        path: offer/findById
        method: get
        cors: true
        authorizer:
          name: offerAuthorizer
          identitySource: method.request.header.Authorization
findOffersByName:
  handler: src/functions/offer/offer.findOffersByName
  events:
    - http:
        path: offer/findByName
        method: get
        cors: true
        authorizer:
          name: offerAuthorizer
          identitySource: method.request.header.Authorization
findOffersByTags:
  handler: src/functions/offer/offer.findOffersByTags
  events:
    - http:
        path: offer/findByTags
        method: get
        cors: true
        authorizer:
          name: offerAuthorizer
          identitySource: method.request.header.Authorization
findOffersByUser:
  handler: src/functions/offer/offer.findOffersByUser
  events:
    - http:
        path: offer/findByUser
        method: get
        cors: true
        authorizer:
          name: offerAuthorizer
          identitySource: method.request.header.Authorization
findOffersByRestaurant:
  handler: src/functions/offer/offer.findOffersByRestaurant
  events:
    - http:
        path: offer/findByRestaurant
        method: get
        cors: true
        authorizer:
          name: offerAuthorizer
          identitySource: method.request.header.Authorization
findOffersByOwner:
  handler: src/functions/offer/offer.findOffersByOwner
  events:
    - http:
        path: offer/findByOwner
        method: get
        cors: true
        authorizer:
          name: offerAuthorizer
          identitySource: method.request.header.Authorization
findOffersByMember:
  handler: src/functions/offer/offer.findOffersByMember
  events:
    - http:
        path: offer/findByMember
        method: get
        cors: true
        authorizer:
          name: offerAuthorizer
          identitySource: method.request.header.Authorization
updateOfferById:
  handler: src/functions/offer/offer.updateOfferById
  events:
    - http:
        path: offer/update
        method: put
        cors: true
        authorizer:
          name: offerAuthorizer
          identitySource: method.request.header.Authorization
deleteOfferById:
  handler: src/functions/offer/offer.deleteOfferById
  events:
    - http:
        path: offer/delete
        method: delete
        cors: true
        authorizer:
          name: offerAuthorizer
          identitySource: method.request.header.Authorization
